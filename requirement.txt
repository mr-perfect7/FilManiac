Movies Explorer (Search + Details) (Sasanka)


APIs: TMDB (API key required)
Requirements:
- Search movies by title, show poster grid with title, year, rating.
- Click → details view (overview, genres, runtime, backdrop).
- Pagination (Prev/Next) for search results.
- Image lazy-loading and skeleton loaders.
- Stretch: Trending today feed and favorites list.
